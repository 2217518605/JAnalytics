[service]
  name = "denim-view"

[deploy]
  startCommand = "PYTHONPATH=src uvicorn src.main:app --host 0.0.0.0 --port $PORT"

[service.env]
  PGDATABASE_URL = "${{Postgres.DATABASE_URL}}"
  OPENAI_MODEL = "deepseek-v4-pro"
  OPENAI_BASE_URL = "https://api.deepseek.com"
  COZE_PROJECT_TYPE = "agent"
